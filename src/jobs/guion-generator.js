const { generarGuionesDesdeIdeas } = require('../services/guiones');

/**
 * Job wrapper para generación de guiones desde ideas
 * Este módulo simplemente exporta la función del servicio
 */
module.exports = {
  generarGuionesDesdeIdeas
};
